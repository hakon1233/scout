# Session — 2026-07-17

## Session — PER-288

**Objective:** Establish why Norwegian tech-industry stories appeared outside the founder's listed interests, reproduce the leak from live persisted state, fix it at the originating layer, and verify a real post-fix brief without regressing feed presentation.

### Test-first classification (initial)

- **Symptom:** A generated brief contains Norwegian tech-industry news that the founder did not explicitly list as an interest.
- **Classification:** STATE_DEPENDENT pending live-state confirmation; the reported output and configured interests are persisted in the founder's companion state.
- **Affected workflow:** Saved interests → per-interest research session → assembled brief → feed.
- **Boundaries:** Interest intent docs and research prompting are the likely fan-out; feed-card/detail rendering must remain unchanged.

### Log

- Cold-start issue with no parent, sibling history, or prior work-log. Related work confirms every saved interest receives its own research session and detail-depth changes only widened each story's body; neither intentionally introduced adjacent-interest or general-news sections.
- Live state: saved topics are `ai startup news`, `harness news`, `ai coding tools & models`, `rosenborg lead agent`, `lundamo lead agent`, and `matt pocock deep dives`. The offending brief `3e5d4ee1-903a-490f-aa89-861a81e038e4` (19:06 UTC) instead records one explicit basis/topic, `norwegian tech industry news`, and contains Eviny/Mer, Telenor/Bahnhof, and Cognite stories. The scheduled run telemetry remains at 18:37 UTC and its five-interest brief is in real history.
- Root cause: the later run was necessarily ephemeral/dry-run (a disjoint non-ephemeral POST would hit the wipe guard or replace saved interests). PER-218 intentionally preserved saved interests/docs but still wrote its result into global `last_brief` for polling; PER-219 excluded ephemeral output from history but not that slot. The app adopts any ready `last_brief` as the feed, so QA output displaced the founder's real edition.
- Hypothesis: preserve ephemeral output for its caller, tag its provenance on the brief, and make feed/health resolution reject tagged slots and fall back to non-ephemeral history. This enforces the listed-interest invariant without removing the intentional QA feature.
