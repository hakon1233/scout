{
  "name": "@scout/agent",
  "version": "0.3.0",
  "description": "Local loopback companion for Scout. Runs an HTTP server on 127.0.0.1 that the Scout web app talks to, and shells out to your local Claude Code CLI (using its built-in WebSearch + WebFetch tools) for research and synthesis. Your Anthropic OAuth token never leaves your machine.",
  "bin": {
    "scout-agent": "./dist/cli.js"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist",
    "webroot",
    "README.md"
  ],
  "scripts": {
    "prebuild": "node ../../scripts/live-build-guard.mjs",
    "build": "tsc -p tsconfig.json && node scripts/write-build-info.mjs",
    "dev": "tsx src/cli.ts",
    "test": "tsx --test test/*.test.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  },
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "public"
  }
}
