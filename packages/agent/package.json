{
  "name": "@notiva/agent",
  "version": "0.2.0",
  "description": "Local loopback companion for Notiva. Runs an HTTP server on 127.0.0.1 that the Notiva web app talks to, fetches articles via Exa, and shells out to your local `claude` CLI for synthesis. Your Anthropic OAuth token never leaves your machine.",
  "bin": {
    "notiva-agent": "./dist/cli.js"
  },
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "files": [
    "dist",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "tsx test/loopback.test.ts",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {},
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  },
  "license": "UNLICENSED",
  "publishConfig": {
    "access": "public"
  }
}
