{
  "name": "@scout/sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Local-only proxy that lets Scout call Anthropic (via Claude Code subscription auth) and Exa without putting API keys in the browser.",
  "type": "module",
  "engines": { "node": ">=20" },
  "bin": { "scout-sidecar": "./dist/cli.js" },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "start": "node dist/cli.js",
    "test": "tsx test/proxy.test.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "@types/node": "^20.0.0"
  },
  "license": "UNLICENSED"
}
